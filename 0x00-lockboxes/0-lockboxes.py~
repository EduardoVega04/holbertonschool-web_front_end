#!/usr/bin/python3
"""Lockboxes, an interview coding problem.
   Determine if all boxes can be opened"""


def canUnlockAll(boxes):
    """Function that evaluates if all boxes can be opened"""
    list_keys = []
    for box in boxes:
        for key in box:
            list_keys.append(box[key])
    print(list_keys)
